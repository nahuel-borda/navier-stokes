  name: Collect Performance Metrics Scaling OMP

  on:
    workflow_dispatch:

  env:
    SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

  jobs:
    job1:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "1"

    job2:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "2"

    job3:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "4"

    job4:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "8"

    job5:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "16"

    job6:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "2" "32"

    job7:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "1"

    job8:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "2"

    job9:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "4"

    job10:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "8"

    job11:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "16"

    job12:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "4" "32"

    job13:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "1"

    job14:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "2"

    job15:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "4"

    job16:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "8"

    job17:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "16"

    job18:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "32" "32"

    job19:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "1"

    job20:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "2"

    job21:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "4"

    job22:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "8"

    job23:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "16"

    job24:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 128 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "128" "64" "32"

    job25:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "1"

    job26:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "2"

    job27:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "4"

    job28:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "8"

    job29:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "16"

    job30:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "2" "32"

    job31:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "1"

    job32:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "2"

    job33:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "4"

    job34:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "8"

    job35:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "16"

    job36:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "4" "32"

    job37:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "1"

    job38:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "2"

    job39:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "4"

    job40:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "8"

    job41:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "16"

    job42:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "32" "32"

    job43:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "1"

    job44:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "2"

    job45:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "4"

    job46:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "8"

    job47:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "16"

    job48:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 256 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "256" "64" "32"

    job49:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "1"

    job50:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "2"

    job51:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "4"

    job52:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "8"

    job53:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "16"

    job54:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "2" "32"

    job55:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "1"

    job56:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "2"

    job57:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "4"

    job58:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "8"

    job59:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "16"

    job60:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "4" "32"

    job61:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "1"

    job62:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "2"

    job63:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "4"

    job64:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "8"

    job65:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "16"

    job66:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "32" "32"

    job67:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "1"

    job68:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "2"

    job69:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "4"

    job70:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "8"

    job71:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "16"

    job72:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 512 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "512" "64" "32"

    job73:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "1"

    job74:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "2"

    job75:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "4"

    job76:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "8"

    job77:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "16"

    job78:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "2" "32"

    job79:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "1"

    job80:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "2"

    job81:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "4"

    job82:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "8"

    job83:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "16"

    job84:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "4" "32"

    job85:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "1"

    job86:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "2"

    job87:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "4"

    job88:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "8"

    job89:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "16"

    job90:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "32" "32"

    job91:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "1"

    job92:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "2"

    job93:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "4"

    job94:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "8"

    job95:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "16"

    job96:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 1024 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "1024" "64" "32"

    job97:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "1"

    job98:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "2"

    job99:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "4"

    job100:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "8"

    job101:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "16"

    job102:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "2" "32"

    job103:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "1"

    job104:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "2"

    job105:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "4"

    job106:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "8"

    job107:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "16"

    job108:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "4" "32"

    job109:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "1"

    job110:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "2"

    job111:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "4"

    job112:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "8"

    job113:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "16"

    job114:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "32" "32"

    job115:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "1"

    job116:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "2"

    job117:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "4"

    job118:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "8"

    job119:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "16"

    job120:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 2048 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "2048" "64" "32"

    job121:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "1"

    job122:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "2"

    job123:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "4"

    job124:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "8"

    job125:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "16"

    job126:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "2" "32"

    job127:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "1"

    job128:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "2"

    job129:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "4"

    job130:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "8"

    job131:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "16"

    job132:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "4" "32"

    job133:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "1"

    job134:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "2"

    job135:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "4"

    job136:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "8"

    job137:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "16"

    job138:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "32" "32"

    job139:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "1"

    job140:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "2"

    job141:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "4"

    job142:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "8"

    job143:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "16"

    job144:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 4096 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "4096" "64" "32"

    job145:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "1"

    job146:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "2"

    job147:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "4"

    job148:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "8"

    job149:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "16"

    job150:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "2" "32"

    job151:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "1"

    job152:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "2"

    job153:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "4"

    job154:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "8"

    job155:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "16"

    job156:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "4" "32"

    job157:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "1"

    job158:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "2"

    job159:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "4"

    job160:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "8"

    job161:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "16"

    job162:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "32" "32"

    job163:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "1"

    job164:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "2"

    job165:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "4"

    job166:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "8"

    job167:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "16"

    job168:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 8192 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "8192" "64" "32"

    job169:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "1"

    job170:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "2"

    job171:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "4"

    job172:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "8"

    job173:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "16"

    job174:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "2" "32"

    job175:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "1"

    job176:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "2"

    job177:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "4"

    job178:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "8"

    job179:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "16"

    job180:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "4" "32"

    job181:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "1"

    job182:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "2"

    job183:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "4"

    job184:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "8"

    job185:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "16"

    job186:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "32" "32"

    job187:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "1"

    job188:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "2"

    job189:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "4"

    job190:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "8"

    job191:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "16"

    job192:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 16384 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "16384" "64" "32"

    job193:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 2 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "1"

    job194:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 2 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "2"

    job195:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 2 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "4"

    job196:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 2 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "8"

    job197:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 2 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "16"

    job198:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 2 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "2" "32"

    job199:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 4 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "1"

    job200:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 4 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "2"

    job201:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 4 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "4"

    job202:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 4 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "8"

    job203:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 4 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "16"

    job204:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 4 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "4" "32"

    job205:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 32 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "1"

    job206:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 32 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "2"

    job207:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 32 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "4"

    job208:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 32 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "8"

    job209:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 32 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "16"

    job210:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 32 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "32" "32"

    job211:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 64 1"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "1"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "1"

    job212:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 64 2"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "2"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "2"

    job213:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 64 4"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "4"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "4"

    job214:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 64 8"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "8"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "8"

    job215:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 64 16"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "16"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "16"

    job216:
      runs-on: ubuntu-latest
      continue-on-error: true

      steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install gspread
        run: pip install gspread oauth2client datetime pytz

      - name: "amd clang -fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra 32768 64 32"
        env:
          pkpass: ${{secrets.PKPASSWORD}}
        run: |
          echo "$SSH_PRIVATE_KEY" > ssh_key
          chmod 400 ssh_key
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H atom.famaf.unc.edu.ar >> ~/.ssh/known_hosts
          echo 'ServerAliveInterval 30' | tee -a ~/.ssh/config
          echo 'ServerAliveCountMax 6000' | tee -a ~/.ssh/config
          ssh -i ssh_key cp202303@atom.famaf.unc.edu.ar 'cd /users/cp2023/cp202303/navier-stokes/; bash run_omp.sh clang "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "32"' > output_amd.txt
          rm ssh_key
          python write_to_sheet.py output_amd.txt $pkpass $GITHUB_SHA "n/d" "amd" "clang" "-fopenmp -std=c99 -march=native -Ofast -ftree-vectorize -Wall -Wextra" "32768" "64" "32"
